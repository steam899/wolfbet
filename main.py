from fastapi import FastAPI\nfrom pydantic import BaseModel\nimport random\n\napp = FastAPI()\n\nclass Bet(BaseModel):\n    amount: float\n    outcome: int\n\n@app.post("/roll")\ndef roll_dice(bet: Bet):\n    result = random.randint(1, 6)  # Roll a die\n    win = result == bet.outcome\n    payout = bet.amount * 2 if win else 0\n    return {\n        "result": result,\n        "win": win,\n        "payout": payout\n    }\n\n@app.get("/")\ndef read_root():\n    return {"message": "Welcome to WolfBet Dice Game API"}